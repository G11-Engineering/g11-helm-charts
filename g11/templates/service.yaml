apiVersion: v1
kind: Service
metadata:
  name: {{ include "g11.fullname" . }}
  labels:
    {{- include "g11.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "g11.selectorLabels" . | nindent 4 }}
