apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "g11.fullname" . }}-test-connection"
  labels:
    {{- include "g11.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "g11.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
